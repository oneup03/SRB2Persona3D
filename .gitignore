/data
*.ncb
*.opt
*.plg
*.suo
*.vcproj.*.*.user
Win32_LIB_ASM_Debug
Win32_LIB_ASM_Release
*.bsc
/SRB2.layout
/SRB2.depend
/src/comptime.h
*.dgb
*.debug
*.debug.txt
*.user
*.db
*.opendb
/.vs
/debian
/assets/debian
/make
/bin

# Build intermediates. src/Makefile refuses to run while a legacy /objs
# directory exists (an MSVC build creates one), so don't let it get committed.
/objs
/src/sdl/objs

# Staged runnable game folder produced by build.sh
/deploy/
# zlib/libpng archives build.sh rebuilds from the bundled sources
/libs/.build64/
# The upstream full installer, if dropped in the repo root to extract assets
/*.exe
